// utils/brent.ts

export function brent(
  f: (x: number) => number,
  lowerBound: number,
  upperBound: number,
  tolerance: number,
  maxIterations: number
): number {
  // ... rest of the provided function ...
}
